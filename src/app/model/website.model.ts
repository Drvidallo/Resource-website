export class Website {
  title: string;
  message: string;
  url: string;

  constructor(title: string, message: string, url: string) {
    this.title = title;
    this.message = message;
    this.url = url;
  }
}
